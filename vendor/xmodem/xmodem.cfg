# Linker config for the XMODEM spike blob: a flat binary loaded at $2000 in RAM.
MEMORY {
    XM: start = $2000, size = $1000, fill = yes, fillval = $00, file = %O;
}
SEGMENTS {
    CODE: load = XM, type = rw, align = $100;
}
