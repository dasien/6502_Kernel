# Build artifacts (regenerated by the xmodem_bin CMake target).
*.o
*.bin
*.map
# xmodem.txt (the pristine original) and xmodem.s (the generated ca65 build
# input, like src/kernel/forth/forth.s) ARE tracked.
